runtime: python39

handlers:
  - url: /static
    static_dir: static

  - url: /favicon.ico
    static_files: /static/favicon.ico
    upload: /static/favicon.ico

instance_class: F4

inbound_services:
- warmup